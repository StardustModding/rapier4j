class Tuple<A, B> {
    bound A: Clone;
    bound B: Clone;

    [rust] field a: A;
    [rust] field b: B;

    static init fn of(first: A, second: B) -> Tuple<A, B>;
    
    fn first() -> A;
    fn second() -> B;
};
